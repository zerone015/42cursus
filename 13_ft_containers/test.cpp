#include <iostream>
#include "map.hpp"
#include <map>
#include <vector>

int main()
{
    // std::map<char, int> map;
    // map.insert(std::pair<char, int>('a', 114));
    // map.insert(std::pair<char, int>('b', 444));

    // std::map<char, int>::iterator a = map.begin();

    // ++a;
    // std::map<char, int>::iterator b = map.end();
    // if (a == b)
    //     std::cout << "asdf" << std::endl;
    // std::map<char, int> map2(a, b);
    // std::cout << map2.size() << std::endl;

}